@model List<ResultSpecialOfferDto>

<div class="col-lg-4">
   @*  @foreach (var item in Model)
    {
        <div class="product-offer mb-30" style="height: 200px;">
            <img class="img-fluid" src="@item.ImageUrl" alt="">
            <div class="offer-text">
                <h6 class="text-white text-uppercase">@item.Title</h6>
                <h3 class="text-white mb-3">@item.SubTitle</h3>
                <a href="" class="btn btn-primary">Şimdi Alışverişe Başla</a>
            </div>
        </div>
    } *@
    

     @for (int i = 0; i < 2; i++)
    {
        <div class="product-offer mb-30" style="height: 200px;">
            <img class="img-fluid" src="@Model[i].ImageUrl" alt="">
            <div class="offer-text">
                <h6 class="text-white text-uppercase">@Model[i].Title</h6>
                <h3 class="text-white mb-3">@Model[i].SubTitle</h3>
                <a href="" class="btn btn-primary">Şimdi Alışverişe Başla</a>
            </div>
        </div>
    }
    
    
</div>